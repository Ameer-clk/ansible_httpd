---
- name: check disk space
  hosts: web 
  tasks: 
  - name: check_disk 
    ansible_builtin.shell: 
      cmd: df -h 
    register:  result 

  - name: Display result
    debug: 
      msg: "{{result.stout_lines}}"
